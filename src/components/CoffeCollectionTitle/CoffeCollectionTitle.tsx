import React from 'react';
import styles from './CoffeCollectionTitle.module.scss';

const CoffeCollectionTitle = () => (
  <h1 className={styles.title}>
    our collection
  </h1>
);

export default CoffeCollectionTitle;
